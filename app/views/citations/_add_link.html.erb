<%= turbo_frame_tag dom_id(event, :citation_form) do %>
  <%= link_to new_person_event_citation_path(person, event), class: "btn btn--plain citation-add" do %>
    <%= icon_tag "add" %> <%= t("citations.add") %>
  <% end %>
<% end %>
