<%# Re-rendered whole on event create/update/destroy (turbo_stream target id "events"). %>
<section class="events" id="events">
  <h2><%= t("events.title") %></h2>
  <% if person.events.any? %>
    <ul class="event-list">
      <%= render partial: "events/event", collection: person.events, locals: { person: person } %>
    </ul>
  <% else %>
    <p class="muted"><%= t("events.none") %></p>
  <% end %>

  <% if can_edit? %>
    <%= turbo_frame_tag "add_event" do %>
      <div class="event-add-actions">
        <%= link_to new_person_event_path(person), class: "btn btn--plain add-relative" do %>
          <%= icon_tag "add" %> <%= t("events.add") %>
        <% end %>
        <%= link_to new_person_event_path(person, kind: "BIRT"), class: "btn btn--plain" do %>
          <%= icon_tag "add" %> <%= t("events.add_birthplace") %>
        <% end %>
        <%= link_to new_person_event_path(person, kind: "RESI"), class: "btn btn--plain" do %>
          <%= icon_tag "add" %> <%= t("events.add_residence") %>
        <% end %>
      </div>
    <% end %>
  <% end %>
</section>
