<% content_for :title, t("people.title") %>

<div class="page-head">
  <h1><%= t("people.title") %></h1>
  <div class="page-head-actions">
    <% if can_edit? %>
      <%= link_to t("people.add"), new_person_path, class: "btn btn--reversed" %>
    <% end %>
    <%= link_to t("trees.clan_link"), clan_tree_path, class: "btn" %>
    <%= link_to t("maps.tree_link"), tree_map_path, class: "btn" %>
    <%= button_to t("exports.create"), export_path, method: :post, class: "btn" %>
  </div>
</div>

<%= form_with url: people_path, method: :get,
      data: { controller: "search", turbo_action: "advance" },
      class: "search-form" do |f| %>
  <div class="search-bar">
    <%= f.search_field :q, value: @q,
          placeholder: t("people.search_placeholder"),
          autocomplete: "off",
          data: { search_target: "input", action: "input->search#debounce" } %>
    <%= f.select :sex,
          Person::SEXES.map { |s| [t("people.sex.#{s}"), s] },
          { include_blank: t("people.sex_filter_all"), selected: @sex },
          class: "search-filter",
          data: { action: "change->search#submit" } %>
    <%= f.select :sort,
          Person::SORT_OPTIONS.map { |key| [t("people.sort.#{key}"), key] },
          { selected: @sort },
          class: "search-filter",
          data: { action: "change->search#submit" } %>
    <% if @q.present? || @sex.present? || @sort != Person::SORT_OPTIONS.first %>
      <%= link_to t("people.search_clear"), people_path, class: "btn btn--plain search-clear" %>
    <% end %>
  </div>
<% end %>

<% if @people.any? %>
  <ul class="people-list">
    <%= render @people %>
  </ul>
<% else %>
  <p class="empty">
    <% if @q.present? || @sex.present? %>
      <%= t("people.search_empty") %>
    <% elsif can_edit? %>
      <%= t("people.empty") %> <%= link_to t("people.add_first"), new_person_path %>.
    <% else %>
      <%= t("people.empty") %>
    <% end %>
  </p>
<% end %>
